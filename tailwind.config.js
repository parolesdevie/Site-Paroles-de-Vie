'use strict'

module.exports = {
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {
      spacing: {
        // 120: '30rem',
        // 150: '37.5rem'
      }
    }
  },
  variants: {
    extend: {
      // opacity: ['hover']
    }
  }
}
